spring:
  application:
    name: exam2
  datasource:
    url: jdbc:mysql://localhost:3300/exam2
    username: root
    password: Vu123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
     ddl-auto: update
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect
      format_sql: true
    show-sql: true
server:
  port: 1008
